const numbers = [10, 20, 30, 40, 50];

// Vag korte forEach() method use kora
numbers.forEach(function (number) {
  const result = number * 2; // Protita element vag kora hoise 2 diye
  console.log(result);
});
